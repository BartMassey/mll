#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <getopt.h>
#include <string.h>

#include "../mll.h"

/*
static char *usage_msg = "learner: hamming: usage: -a hamming";

static void usage(void) {
    fprintf(stderr, "%s\n", usage_msg);
    exit(1);
}
*/

struct params *parseargs_hamming(int argc, char **argv) {
    return NULL;
}

